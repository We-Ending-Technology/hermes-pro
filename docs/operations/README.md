# Operações

Use `.env.example` como referência sem copiar segredos para o repositório. No Render, configure variáveis protegidas no painel. O endpoint `/health` é o health check do serviço web. O worker é separado e pode ser reiniciado sem alterar o serviço HTTP.
