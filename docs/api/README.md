# API inicial

`GET /health` retorna o estado do serviço. `GET /api/v1/agents` lista agentes registrados. `POST /api/v1/agents/run` executa um agente stub e recebe `{ "agent": "diagnostic", "input": {} }`.
