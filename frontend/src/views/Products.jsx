import StatusBadge from "../components/StatusBadge";
export default function Products({ products, onCreate }) { return <section className="panel"><div className="section-head"><div><span className="kicker">PRODUCT STUDIO</span><h2>Produtos</h2></div><button className="secondary" onClick={onCreate}>Novo</button></div>{products.length===0 ? <div className="empty"><strong>Nenhum produto ainda.</strong><p>Comece com um tema específico. O sistema criará um job assíncrono e mostrará o estado real.</p><button className="primary" onClick={onCreate}>Começar</button></div> : <div className="product-list">{products.map(p => <article className="product-card" key={p.id}><div><span className="kicker">{p.current_stage}</span><h3>{p.title || p.topic}</h3><p>{p.topic}</p></div><StatusBadge status={p.status}/></article>)}</div>}</section>; }
