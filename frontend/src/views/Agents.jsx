export default function Agents({ agents }) { return <section className="panel"><span className="kicker">AGENT GRID</span><h2>Agentes</h2><p>Cada agente deve ter uma função observável e estado verificável.</p><div className="agent-grid">{(agents||[]).map(name=><article key={name}><span>AGENT</span><strong>{name}</strong><small>Disponível no gateway atual</small></article>)}</div></section>; }
